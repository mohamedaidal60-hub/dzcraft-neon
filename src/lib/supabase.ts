// Supabase client has been replaced by Neon Postgres.
// All database operations are now handled via the Express server API (/api/...)
// This file is kept only to prevent import errors during transition, but exports null.

export const supabase = null;
export default null;
